import { useEffect } from "react";

function XpOverTime() {
  useEffect(() => {
    const fetchXp = async () => {
      const query = `{
        
      }`;
    };
    fetchXp();
  }, []);

  return <div></div>;
}

export default XpOverTime;
